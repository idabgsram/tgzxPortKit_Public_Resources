// ============================================================================
//  base_devices/sdk0/sample_device/system_ext.rhai
// ============================================================================
// system_ext is an Android 10+ partition that holds non-core system
// extensions — mostly OEM-specific privapps + config. Most devices don't
// need anything here; this stub exists so new users can see where the file
// goes and what the empty-but-correct shape looks like.

log("system_ext.rhai (sample_device) noop");
