// ============================================================================
//  common/global/vendor.rhai — universal vendor tweaks (rare)
// ============================================================================
// Most vendor edits belong to the specific device. This script exists so
// you can see where a global vendor hook LIVES even when it's empty.

log("common/global vendor.rhai noop");
